name: Quality (search service)

on:
  push:
    paths:
      - 'services/search/**'
  pull_request:
    paths:
      - 'services/search/**'

jobs:
  quality:
    uses: ./.github/workflows/quality-reusable.yml
    with:
      service-path: services/search
      service-name: search
