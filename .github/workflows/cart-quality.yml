name: Quality (cart service)

on:
  push:
    paths:
      - 'services/cart/**'
  pull_request:
    paths:
      - 'services/cart/**'

jobs:
  quality:
    uses: ./.github/workflows/quality-reusable.yml
    with:
      service-path: services/cart
      service-name: cart
