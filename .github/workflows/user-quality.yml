name: Quality (user service)

on:
  push:
    paths:
      - 'services/user/**'
  pull_request:
    paths:
      - 'services/user/**'

jobs:
  quality:
    uses: ./.github/workflows/quality-reusable.yml
    with:
      service-path: services/user
      service-name: user
